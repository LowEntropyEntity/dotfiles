#!/usr/bin/env bash

DOTFILES_GIT_WORK_TREE="${DOTFILES_GIT_WORK_TREE:-$HOME}"
DOTFILES_GIT_DIR="${DOTFILES_GIT_DIR:-$DOTFILES_GIT_WORK_TREE/.dotfiles/.git}"

git --git-dir=$DOTFILES_GIT_DIR --work-tree=$DOTFILES_GIT_WORK_TREE checkout -f
