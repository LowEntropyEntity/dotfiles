#!/usr/bin/env bash

DOTFILES_WORK_TREE="${DOTFILES_WORK_TREE:-$HOME}"
DOTFILES_GIT_DIR="${DOTFILES_GIT_DIR:-$DOTFILES_WORK_TREE/.dotfiles/.git}"

git --git-dir=$DOTFILES_GIT_DIR --work-tree=$DOTFILES_WORK_TREE checkout -f
