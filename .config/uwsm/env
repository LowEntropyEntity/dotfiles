export OZONE_PLATFORM=wayland
export XCURSOR_SIZE=24
export XDG_CURRENT_DESKTOP=Hyprland
export XDG_SESSION_TYPE=wayland

if lspci | grep -qi nvidia; then
	source ${XDG_CONFIG_HOME}/uwsm/env-nvidia
fi
